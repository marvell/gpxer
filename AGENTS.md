Default to using Bun instead of Node.js.

Assume this application's development server is already running on port 3000 by default. If it is not running and you need to verify the application works, ask the user to start the server instead of starting it yourself.
